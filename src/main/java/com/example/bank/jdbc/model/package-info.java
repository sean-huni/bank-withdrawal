/**
 * Persistence models only: JPA entities / Spring Data JDBC aggregates extending BaseEntity.
 * The only package allowed to import jakarta.persistence.* or org.springframework.data.*.
 */
package com.example.bank.jdbc.model;
