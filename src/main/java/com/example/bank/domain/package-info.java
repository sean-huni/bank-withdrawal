/**
 * Pure business objects used for interaction within the app — sits between the controller
 * and data layers. No persistence dependencies (jakarta.persistence.* / org.springframework.data.*).
 */
package com.example.bank.domain;
