<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="003-create-idempotency-records-table" author="sean">
        <!-- Re-run safety (file-rename identity lesson): no-op when the table already exists. -->
        <preConditions onFail="MARK_RAN">
            <not><tableExists tableName="idempotency_records"/></not>
        </preConditions>
        <createTable tableName="idempotency_records">
            <column name="id" type="UUID" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="idempotency_key" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="request_fingerprint" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(16)">
                <constraints nullable="false"/>
            </column>
            <column name="response_body" type="TEXT"/>
            <column name="response_type" type="VARCHAR(256)"/>
            <column name="version" type="BIGINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMPTZ" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMPTZ" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <addUniqueConstraint tableName="idempotency_records" columnNames="idempotency_key"
                             constraintName="uk_idempotency_key"/>
        <rollback>
            <dropTable tableName="idempotency_records"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
