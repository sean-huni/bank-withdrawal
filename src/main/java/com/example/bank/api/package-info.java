/**
 * HTTP boundary plumbing only — exception advice ({@code advice/}), request
 * filters ({@code filter/}) and parameter validation ({@code validation/}).
 * Controllers themselves live in {@code com.example.bank.ctl}; boundary DTOs in
 * {@code com.example.bank.dto}.
 */
package com.example.bank.api;
